'use strict';

/**
 * Module dependencies.
 */
var travels = require('../../app/controllers/TravelControllers');

module.exports = function (app) {
    // User Profile Routes
    // app.route('/profile/:profileId')
    // .get(profiles.show)
    // .put(profiles.updateProfile, profiles.updateAddress);

    // app.route('/profile')
    // .post(profiles.create, profiles.createProfileAccount);

    // app.param('profileId', profiles.getProfileId);

    // Travel Routes
    // app.route('/travel/:tprofileId')
    // .get(travels.allTravel);

    // app.param('tprofileId', travels.allTravel);
};